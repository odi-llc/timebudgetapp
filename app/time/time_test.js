'use strict';

describe('myApp.time module', function() {

  beforeEach(module('myApp.time'));

  describe('time controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var timeCtrl = $controller('TimeCtrl');
      expect(timeCtrl).toBeDefined();
    }));

  });
});